version: '2'

sql:
  - engine: sqlite
    queries: queries.sql
    schema: schema.sql
    gen:
      go:
        package: gen
        out: gen
        emit_json_tags: true
        emit_empty_slices: true
